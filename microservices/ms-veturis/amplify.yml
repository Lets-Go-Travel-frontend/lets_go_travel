version: 1
backend:
  phases:
    preBuild:
      commands:
        - npm ci
    build:
      commands:
        - npx tsc
    postBuild:
      commands:
        - pm2 reload ecosystem.config.js || pm2 start ecosystem.config.js
artifacts:
  baseDirectory: dist
  files:
    - '**/*'
